# I'll work on a Dockerfile later. I mean, why not just utilize a cronjob? That's the plan :)
# Also, I'm going to used a Ampere Oracle instance so, got to build it for ARM64.
FROM --platform=arm64 alpine:latest 
